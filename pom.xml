<settings>
  <servers>
    <server>
      <id>github-packages-releases</id> <!-- Must match the ID in your `pom.xml` -->
      <username>lithesheeli</username> <!-- This can be a placeholder -->
      <password>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</password> <!-- Use environment variable for security -->
    </server>
    <server>
      <id>github-packages-snapshots</id> <!-- Must match the ID in your `pom.xml` -->
      <username>lithesheeli</username> <!-- This can be a placeholder -->
      <password>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</password> <!-- Use environment variable for security -->
    </server>
  </servers>
  <profiles>
    <profile>
      <id>github-packages</id>
      <repositories>
        <repository>
          <id>github-packages-releases</id> <!-- Match ID from `distributionManagement` -->
          <url>https://maven.pkg.github.com/lithesheeli/gitops</url> <!-- URL for releases -->
        </repository>
        <repository>
          <id>github-packages-snapshots</id> <!-- Match ID from `distributionManagement` -->
          <url>https://maven.pkg.github.com/lithesheeli/gitops</url> <!-- URL for snapshots -->
        </repository>
      </repositories>
    </profile>
  </profiles>
  <activeProfiles>
    <activeProfile>github-packages</activeProfile>
  </activeProfiles>
</settings>
